"use client";

import { formatDateTime } from "@/lib/format";
import { useTranslation } from "@/lib/i18n/use-translation";

interface ReportFooterProps {
  generatedBy: string;
  generatedAt: string;
  filterSummary?: string;
}

export function ReportFooter({ generatedBy, generatedAt, filterSummary }: ReportFooterProps) {
  const { t } = useTranslation();
  return (
    <footer className="mt-[var(--mds-space-8)] border-t border-border pt-[var(--mds-space-4)] text-xs text-muted-foreground">
      <div className="flex flex-wrap justify-between gap-[var(--mds-space-2)]">
        <span>
          {t("Generated by")}: {generatedBy} · {formatDateTime(generatedAt)}
        </span>
        {filterSummary ? (
          <span>
            {t("Filters")}: {t(filterSummary)}
          </span>
        ) : null}
      </div>
    </footer>
  );
}
